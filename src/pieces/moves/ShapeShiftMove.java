package pieces.moves;

import pieces.Position;

public class ShapeShiftMove extends Move {
    public ShapeShiftMove(Position startPosition, Position endPosition) {
        super(startPosition, endPosition);
    }
}
